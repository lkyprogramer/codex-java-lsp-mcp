# JIN Phase N5：工具面 + live A/B（2026-08-21）

## 判定（N5-01 GO；N5-02 harness GO；live **BLOCKED_EXTERNAL**）

| 项 | 结果 |
|---|---|
| `java_context` 注册 | **GO**（6 工具，`java_impact` 仍在） |
| `measure:tool-schema` | **GO** 564 token（≤ 600） |
| 三臂 harness | **GO** 6 holdout × old/jin/serena × ABC/CBA = 36 cells |
| live 三臂 A/B | **BLOCKED_EXTERNAL** TaskSuccess/usage **UNMEASURED**（不是 0） |
| endpoint | host `192.168.10.29:28343` `/v1/models` 无 key → HTTP **401**（主机在，缺凭据） |
| Serena | **UNAVAILABLE**（`SERENA_MCP_COMMAND` 未设；不伪造工具面） |
| 合 main | 否 |
| 决策 | N5 **PARTIAL**；0A.4(a) escalate；**不进 N6** |

`--authorize-external --execute-live` 已跑，exit 3。进程环境无 `OPENAI_API_KEY` / `JAVA_LSP_AGENT_API_KEY` / `ANTHROPIC_API_KEY`。未发 live 流量。未发明 TaskSuccess。

N5 退出条件（TaskSuccess_new ≥ current、tool rounds/token 下降、闭合一条 live false）**未测量**。Range 残差与 RSS 512 MiB 仍开。

## N5-01

handler `src/tools/java-context.ts`。T1 dist 1146 / scripts 190 fail 0。smoke GO。code `b0c08dd`。

## N5-02

harness：`scripts/run-agent-trace-matrix.mjs` / `run-agent-trace-live.mjs`。T1 dist 1146 / scripts 193 fail 0。code `78e01a3`。executableTree `81242b54e9789ca28df05287016815868b226c6f`。

解阻：同一 host + model `openclaw/Qwen3.8-27B-WORK` + 进程环境 key + 可选 `SERENA_MCP_COMMAND`，再 `--authorize-external --execute-live --max-tasks 6`。
