# JIN Phase N5：工具面 + live A/B（2026-08-21）

## 判定（N5-01 `java_context` GO；N5-02 未跑）

| 项 | 结果 |
|---|---|
| `java_context` 注册 | **GO**（`PUBLIC_JAVA_TOOLS` 6 工具，`java_impact` 仍在） |
| intent / 无锚点 / navigate | **GO** |
| `measure:tool-schema` `java_context` 描述+schema | **GO** 564 token（≤ 600） |
| T1 | dist 1146 / scripts 190 fail 0 |
| `npm run smoke` | **GO**（tools 含 `java_context`） |
| 三臂 live A/B | **未开工**（需 `--authorize-external`） |
| 合 main | 否 |
| 决策 | N5-01 COMPLETE；N5 整体未出口 |

`src/tools/context.ts` 已是 `ToolContext`，handler 落在 `src/tools/java-context.ts`（0A.4 自决）。生产 factory / `java_impact` / `java_context` / `application.ts` 不读 `JAVA_LSP_ENGINE`。V5R `ATTESTED_PUBLIC_TOOLS` 仍是五工具冻结面。

code `b0c08dd`。executableTree `d99a7ee8…`。

**不合 main。** 不得发明 TaskSuccess。第四仓 holdout 逐场景结果终验前不看。
