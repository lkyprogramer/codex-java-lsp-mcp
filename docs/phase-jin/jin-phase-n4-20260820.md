# JIN Phase N4：EvidenceBundle Planner（2026-08-20）

## 判定

| 轴 | 结果 |
|---|---|
| Planner §16.2 T0 | **GO** |
| T2 discovery mustHit ≥ 0.95 | **GO** 1.000 / 1.000 / 0.962 |
| T2 selectedRate（有 span） | 0.563 / 0.759 / 0.577（train 未看 holdout） |
| T3 token P50 ↓ ≥ 25% 且 ≤ Sprint0' | **FAIL** 35441 / 20718 / 21888 vs old 3393 / 3186 / 2830 |
| T3 RangeLineRecall 不回退 | **FAIL** 0.521 / 0.850 / 0.483 vs 0.842 / 0.875 / 0.853 |
| T3 holdout rReadMust 均值 ≥ 0.75 | **FAIL** 0.375 / 0.525 / 0.200，均值 **0.367** |
| T3 p95 §16.3 | **FAIL** 比 3.281 / 0.329 / 0.466；lishuedu 变慢 |
| T1 | dist 1107 / scripts 190 fail 0（隔离；63c172f） |
| 合 main | 否 |
| 四轴 | **0/4**。首次 T3，2 周迭代窗未到期，**停在 N4** 不进 N5 |

## 原因

`JAVA_LSP_ENGINE=jin` 矩阵臂把图搜索发现的路径写进 compact `contexts[]`（无 span 也进候选），并把 hop≤2 的方法窗口尽量装满。结果：

- recall 上升（0.80/0.84/0.70 → 0.93/0.98/0.88）
- pRead 崩溃（大量近邻非 golden 文件进 read set）
- wire+span 把 `estimatedTokens` 拉到 2–3 万
- lishuedu 规划/图遍历 p95 774ms vs old 236ms

生产 MCP / `java_impact` **不读** `JAVA_LSP_ENGINE`。

## 产物 SHA-256

| 文件 | SHA-256 |
|---|---|
| `jin-n4-t3-summary.json` | `9376aa952b1bc4734e390e3f6aae662e55d658323fbcb1e8ddd007dc1f925344` |
| `jin-n4-planner-replay.json` | `2680fabcbe305ca225d12251d4462625fbbd94ad478539b4955c07fd708dd0f6` |

raw cell 不入库。T3 输出目录 `/tmp/jin-n4-t3-20260820224922`。

## 怎么读

1. old = 当前 compact `java_impact`；new = 同树 `JAVA_LSP_ENGINE=jin`。
2. 不得把 RangeLineRecall=1.0 或 holdout rReadMust=1.0 当成过门。本次也远未到。
3. 不得发明 TaskSuccess。未合 `main`。
4. 下一刀：compact 只输出 selected spans；hop≤2 全收改为预算内 greedy；再跑 T3。
