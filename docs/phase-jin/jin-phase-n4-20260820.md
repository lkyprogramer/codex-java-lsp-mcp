# JIN Phase N4：EvidenceBundle Planner（2026-08-21）

## 判定（span：证明路径整 method，丢掉 SOURCE_ROOT，义务优先 hop-order，预算 2000）

| 轴 | 结果 |
|---|---|
| Planner §16.2 T0 | **GO** |
| T1 | dist 1115 / scripts 190 fail 0 |
| T3 token P50 ↓ ≥ 25% 且 ≤ Sprint0' | **GO** 1609 / 1784 / 1598 vs 3393 / 3186 / 2830（−52.6 / −44.0 / −43.5%） |
| T3 RangeLineRecall 不回退 | **FAIL** 0.534 / 0.850 / 0.533 vs 0.842 / 0.875 / 0.853 |
| T3 holdout rReadMust 均值 ≥ 0.75 | **FAIL** 0.375 / 0.325 / 0.300，均值 **0.333** |
| T3 p95 §16.3（配对 vs 当前 compact） | **GO** 比 0.276 / 0.124 / 0.133。lishuedu warm p95 64ms 仍 > 50ms |
| 合 main | 否 |
| 四轴 | **2/4**。2 周窗未到期，**停在 N4** |

相对 mustHit hop-fill（2641 / 2562 / 2192，token FAIL）：不再把 SOURCE_ROOT 当文件，hop>0 只留证明路径 method / type span，预算 2000。token 与 p95 过门。Range 从 0.384 抬到 0.534 仍回退；holdout 均值 0.300 → 0.333。selected-only 保持。

`storage-signed-url` 上 `AliyunOssGateway` 162–181 已覆盖（该场景 RangeLineRecall=1.0），但仍缺 SignedUrl DTO mustHit。

## 产物 SHA-256

| 文件 | SHA-256 |
|---|---|
| `jin-n4-t3-summary.json`（第一次） | `9376aa952b1bc4734e390e3f6aae662e55d658323fbcb1e8ddd007dc1f925344` |
| `jin-n4-t3-retry-summary.json` | `78cab579e43b5679918f85a591b97cdf3d60e9ca69c9e4e148b2e72856a4d0a3` |
| `jin-n4-t3-pack-summary.json` | `c46d811772533e34b022a06828687fe3d1a9b5b3b96dd87819faef6b3904c3ce` |
| `jin-n4-t3-musthit-summary.json` | `c8db2d8cd5b7879a2ce4f25819f4c36fd39f6a4857791290c17fc6643354ec99` |
| `jin-n4-t3-span-summary.json` | `ec9df52c298c002c21097abc0716880d4a17cda624f92a8a646295b250166dcd` |
| `jin-n4-planner-replay.json` | `2680fabcbe305ca225d12251d4462625fbbd94ad478539b4955c07fd708dd0f6` |

raw cell 不入库。span 矩阵在隔离 scratch `jin-n4-t3-span2`。executableTree `c1523a57`。loadavg1 8.65。

## 怎么读

1. old = 当前 compact `java_impact`；new = 同树 `JAVA_LSP_ENGINE=jin`。
2. 不得把 RangeLineRecall=1.0 / holdout rReadMust=1.0 当成过门。
3. 不得发明 TaskSuccess。未合 `main`。生产 MCP 不读 `JAVA_LSP_ENGINE`。
4. 下一刀：保持 selected-only 与 25% token 门；补的是 discovery/义务闭合（mustHit 文件没进池），不是再多选文件。
