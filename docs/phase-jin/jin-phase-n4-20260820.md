# JIN Phase N4：EvidenceBundle Planner（2026-08-21）

## 判定（hop0 字段 callee cap + DTO type span，token+p95 仍 2/4）

| 轴 | 结果 |
|---|---|
| Planner §16.2 T0 | **GO**（含在 isolated T1） |
| T1 | dist 1127 / scripts 190 fail 0 |
| T3 token P50 ↓ ≥ 25% 且 ≤ Sprint0' | **GO** 1730 / 1406 / 1782 vs 3393 / 3186 / 2830（−49.0 / −55.9 / −37.0%） |
| T3 RangeLineRecall 不回退 | **FAIL** 0.582 / 0.850 / 0.558 vs 0.842 / 0.875 / 0.853 |
| T3 holdout rReadMust 均值 ≥ 0.75 | **FAIL** 0.375 / 0.225 / 0.300，均值 **0.300** |
| T3 p95 §16.3（配对 vs 当前 compact） | **GO** 比 0.333 / 0.139 / 0.160 |
| 合 main | 否 |
| 本轮四轴 | **2/4** |

exam-score hop0 1509→1117，剩余约 883。SchoolQueryService 两方法合计仍未进选中集（仍 1 文件）。SignedUrl DTO、MeQueryService、ExcelGenerator 仍缺。AliyunOssGateway + PaperAccessService 仍在。按方法拆包会打 Range，未用。全局 extraNames 灌 call 名未用。

**N4 时钟未到期。** 不进 N5、不合 main。

## 产物 SHA-256

| 文件 | SHA-256 |
|---|---|
| `jin-n4-t3-holdout-hop2-summary.json` | `d21c21fb856f8aa6ab075342e3c90eb74801c32522f1a3836245217fd0f01c0a` |
| `jin-n4-t3-musthit-summary.json` | `34cdbf99d06646ca10a05bb131e628fe5cef4917a45e0ce6a66146cc5b4218f0` |

raw cell 不入库。矩阵 scratch `jin-n4-t3-musthit3`。code HEAD `c7b2482`。T3 executableTree `bce81f365cf0d3446c9a4db4c56d15cf7bb13d92`。loadavg1 4.13。

## 怎么读

1. old = 当前 compact `java_impact`；new = 同树 `JAVA_LSP_ENGINE=jin`。
2. 不得把 RangeLineRecall=1.0 / holdout rReadMust=1.0 当成过门。
3. 不得发明 TaskSuccess。未合 `main`。生产 MCP 不读 `JAVA_LSP_ENGINE`。
4. JIN-N4-04 窗口未到期；2/4 < 3/4，不进 N5。
