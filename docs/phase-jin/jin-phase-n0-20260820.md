# JIN Phase N0：冻结与减债（2026-08-20）

## 判定

| 轴 | 结果 |
|---|---|
| 基线 tag | **GO** `v5r-evidence-baseline` = `aa4098f` |
| T3 identity vs tag | **GO**（recall / pRead / rReadMust / range / holdout / tokens 逐位相同，token P50 Δ=0） |
| 绝对 1.0 residual | 仍 FAIL（预期，不是本卡杀死条件） |
| 生产 LOC | 39466 → **37410**（−2056） |
| 合 main | 否 |

## 身份

| 侧 | 值 |
|---|---|
| old | tag `v5r-evidence-baseline` / `aa4098f` / tree `a045b6dc` |
| new | N0-02 删除后树 `67184b53` |
| 主机 load | 19.55 < 20，必须跑 |
| T1 | dist 1040 / scripts 185 fail 0，`JDTLS_BIN=/usr/bin/false` |

## 产物

| 文件 | SHA-256 |
|---|---|
| `docs/phase-jin/jin-n0-t3-summary.json` | `c5f207178392b2c88e41cc25e005219b32d018766a45eec4a7055831f7490c3c` |
| `run-manifest.json`（checkout 外） | `b34e95516a1a1be264695932e3888284edca0f8ad22efd13cf847e1dc0e3c06a` |

raw cell 不入库。

## 怎么读

1. 默认 `java_impact` 选择/排序没动。删掉的是 default-off 证伪模块（frontier/packing/session/V7/bundle RPC）。
2. ReadUnit builder 等仍挂在默认路径上，N0-02 失败处置：记入 `deferredToN6`，不阻塞出口。
3. 四条 `JAVA_LSP_*` flag 在 `src/` 与 `scripts/` 零命中。
