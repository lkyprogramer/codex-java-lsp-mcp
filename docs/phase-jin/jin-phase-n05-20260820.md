# JIN Phase N0.5：紧凑合同 + 实体入口（2026-08-20）

## 判定

| 轴 | 结果 |
|---|---|
| 紧凑 wire | **GO** `contexts[]` 替换 `files[]+readPlan[]`；diagnostic 仍 V6 |
| T3 质量 identity vs N0 | **GO** recall / pRead / rReadMust / RangeLineRecall / holdout 逐位相同 |
| T3 token P50 | **GO** 三仓下降 27.6% / 28.3% / 27.3%（门 ≥ 20%） |
| T3 配对 p95 | **GO** 0.882 / 1.000 / 1.032 ≤ 1.10 |
| 绝对 1.0 residual | 仍 FAIL（预期，与 N0 相同，不是本卡杀死条件） |
| 实体入口 T2 | **GO** 30/30 top-3 命中 anchor 文件（门 ≥ 80%） |
| 合 main | 否 |

## 身份

| 侧 | 值 |
|---|---|
| old | N0 出口 `09772b2` / tree `fc39b9cd` |
| new | compact serializer `63859d8` + N0.5 patch / executableTree `3a1b3716` |
| 主机 load | 37.66 ≥ 20，runner 仍执行（refuse=false）；p95 未超 1.10 |
| T1 | dist 1054 / scripts 185 fail 0，`JDTLS_BIN=/usr/bin/false` |

## Token 降幅

| 仓 | old P50 | new P50 | 降幅 |
|---|---|---|---|
| lishuedu | 4683 | 3393 | 27.55% |
| cipherlink | 4443 | 3186 | 28.29% |
| exam-parent-v3 | 3890 | 2830 | 27.25% |

## 产物

| 文件 | SHA-256 |
|---|---|
| `docs/phase-jin/jin-n05-t3-summary.json` | `2add6aecc20093f0a44cb2ae3626a65e3d09c99957946a4fd8ea569e3120172f` |
| `docs/phase-jin/jin-entity-entry-replay.json` | `45372ecec0b2c26b1153ef7afbc55591f45d24a9e94908e76e9cd7db41ff0a27` |
| `run-manifest.json`（checkout 外） | `76d1756900a84e22ad2f46ac000ec158bf34d034e058204b04e9c9bf859d7d1c` |

raw cell 不入库。独立 verifier exit 1 = 绝对 1.0 residual。

## 怎么读

1. 默认 `java_impact` **排序/选择未改**。standard/compact 只换 serializer：`contexts[{path,role,proof,spans{s,e,b}}]`。slim `semantic`/`freshness` 留给 cold-nolsp 证明，不是 `files[]+readPlan[]` 双表达。
2. `QUERY_ENTITY_SEARCH` 只在 JavaIndex worker；公开 MCP schema 未加工具。四层回退：FQN → 类型 simpleName 整词 → identifier BM25 → chunk BM25。
3. 规范写「159 条冻结场景」；仓库 golden jsonl 实际是每仓 10 条、共 30。回放 query = `scenario.name` + `taskKeywords`。
4. 生产 LOC 37410 → 38181（+771）。未合 `main`。下一阶段 **N1** knowledge graph。
