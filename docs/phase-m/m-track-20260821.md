# M 轨道出口（M0–M5，2026-08-21）

**判定：PARTIAL。不合 `main`。** Identity 硬门 GO。G1/G2/G4/G5 未达书面目标，按各卡失败处置记账，门未放宽。S1–S4 与 LOC 达标。JIN N1 `0A.4(4b)` RSS 升级项由本轨道 §3 分解门 **RESOLVED**。

## 判定表

| 门 | 目标 | 出口实测 | 结果 |
|---|---|---|---|
| T3 identity | recall/pRead/rReadMust/RangeLineRecall/holdout/tokens 逐位相同 | 三仓全部相同 | **GO** |
| T3 p95 | 配对 ≤ 1.10 | 0.976 / 0.971 / 1.005 | **GO** |
| G1 heap | lishuedu ≤ 200；中小仓 ≤ 64 | 273 / 45 / 78 | **MISS**（M2 处置：记实测下限，不放宽 200） |
| G2 RSS | lishuedu ≤ 400 | 文件-only heap 273；S1 runtime RSS ~477；同进程水位 2045 | **MISS**（M3 处置） |
| G3 冷建 | child ≤ 1536 且 parent Δ ≤ 100 | 110 / 0 | **GO** |
| G4 可服务 | ≤ 2s | lishuedu 2114ms；S4 回热 108ms | **MISS**（含 graph unpack） |
| G5 warm p95 | ≤ +10% vs M0 | 首查询 hydrate ~7.7s；同进程后续 38–76ms | **MISS**（惰性 rest 首触） |
| S1 | ≤ 1 GiB | 733 | **GO** |
| S2 | ≤ 1.4 GiB | 1291（无段共享） | **GO** |
| S3 | 同时冷建 = 1 | `BUILD_SLOT` T0 | **GO** |
| S4 | hibernate ≤ 32 MiB，回热 ≤ 2s | 9 MiB / 108ms | **GO** |
| LOC | 净增 ≤ +2500 | +2118（41971 → 44089） | **GO** |

## 身份表

| 侧 | 值 |
|---|---|
| old | pre-M `1f9eed4` / tree `1f9de664` |
| new | M4 HEAD `70f0939` / executableTree `a2acd89e` |
| 主机 load | 25.89 ≥ 20，`refuse=false`，仍执行 |
| 可用内存 | 8.44 GiB ≥ 4 GiB |
| T1 | dist 1173 + scripts 213 fail 0，`JDTLS_BIN=/usr/bin/false` |
| facts digest vs M0 | 三仓 SHA 不变（lishuedu `1fe63bca…`） |

### T3 逐仓

| 仓 | recall | pRead | rReadMust | RangeLineRecall | tokens P50 | holdout rReadMust | p95Ratio |
|---|---|---|---|---|---|---|---|
| lishuedu | 0.7986 = 0.7986 | 0.7217 = 0.7217 | 0.925 = 0.925 | 0.8419 = 0.8419 | 3393 = 3393 | 0.625 = 0.625 | 0.976 |
| cipherlink | 0.8390 = 0.8390 | 0.6633 = 0.6633 | 0.910 = 0.910 | 0.875 = 0.875 | 3186 = 3186 | 0.550 = 0.550 | 0.971 |
| exam-parent-v3 | 0.6970 = 0.6970 | 0.6000 = 0.6000 | 0.880 = 0.880 | 0.8525 = 0.8525 | 2830 = 2830 | 0.400 = 0.400 | 1.005 |

独立 `verify-three-repo-cold-matrix` exit 1 = 绝对 RangeLineRecall `min===1` 残差（与 JIN N0/N0.5 相同），**不是** old-vs-new identity 失败。

## SHA-256

| 产物 | SHA-256 |
|---|---|
| T3 `matrix-summary.json`（scratch，raw cell 不入库） | `d878395bf00a5357d79b50403502bd892615a04768e76a848fd126c089e61840` |
| T3 `run-manifest.json` | `ed68f414e6713120c2636339d833a5eeffa1ceea78e747496941c4beffec6280` |
| T1 log | `614c4b2c93fde2cf3e6c72ea301a7cd2a388974b9588a91f8f1699bef349075c` |
| G/S 复测 `docs/phase-m/m5-memory.json` | `a6db1695e6eb526a3d5049d465a5a838dd751a557971527e73f2c38cbe0103e6` |
| LOC scratch `m5-loc.json` | `ea7df6396d2866ea84676f2540430f20fb6b3c1eb9576e7fee989742bf37e214` |

## 怎么读

1. 默认 `java_impact` 排序/选择/输出未改。公开 MCP 仍 6 工具。生产路径不读 `JAVA_LSP_ENGINE`。
2. 存储真源改成列式 + v4 分段；查询面仍物化成原来的 fact 对象。M1–M3 facts digest 与 T3 矩阵共同锁 identity。
3. 冷建可走子进程（隔离验证需 `JAVA_LSP_COLD_BUILD_CHILD=1`）；全机 `BUILD_SLOT=1`。闲置 5 分钟 `HIBERNATE`，15 分钟 teardown。
4. 同仓 worktree v4 段共享在 M4 **放弃**。S2 按各持一份惰性 base 测量。
5. 六连提交：`229dff7` M0 → `de804b2` M1 → `0b993fa` M2 → `92b0593` M3 → `70f0939` M4 → 本 closeout。
6. 不进 N6，不重跑 N5 live，不合 `main`。不得发明 TaskSuccess。
